<form method="post" action="{{ path('app_backend_topics_delete', {'categoryId': topic.categoryId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ topic.categoryId) }}">
    <button class="btn">Delete</button>
</form>
