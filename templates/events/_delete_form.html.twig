<div class="row">
    <div class="col">
        <form method="post" action="{{ path('app_events_delete', {'idEvent': event.idEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.idEvent) }}">
            <button class="btn btn-danger">Delete</button>
        </form>
    </div>
    <!-- Ajoutez d'autres colonnes pour plus de boutons si nécessaire -->
</div>
