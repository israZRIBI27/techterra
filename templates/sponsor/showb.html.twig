{% extends 'backendbase.html.twig' %}

{% block title %}Sponsor{% endblock %}

{% block body %}
    <h1>Sponsor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sponsor.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sponsor.nom }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ sponsor.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sponsor_list') }}">back to list</a>

    <a href="{{ path('app_sponsor_editb', {'id': sponsor.id}) }}">edit</a>

    {{ include('sponsor/_deleteb_form.html.twig') }}
{% endblock %}
